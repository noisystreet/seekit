# Cargo 配置
# 参见: https://doc.rust-lang.org/cargo/reference/config.html

[net]
# 若在国内，可取消注释以使用镜像源加速
# git-fetch-with-cli = true

# HTTP 代理（按需取消注释）
# [http]
# proxy = "http://localhost:8118"
# [https]
# proxy = "http://localhost:8118"

[build]
# 默认编译参数
rustflags = ["-D", "warnings"]

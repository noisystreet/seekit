# SearXNG settings.yml — optimized for seekit CLI
use_default_settings: true

general:
  instance_name: "seekit SearXNG"
  debug: false
  privacypolicy_url: false
  contact_url: false
  enable_metrics: false

server:
  port: 8080
  bind_address: "0.0.0.0"
  secret_key: "dc783ff6171f04c83aab296c49c9bffe6a1a1cf1912c676809aa977f18c1eccd"  # ⚠️ Replace with your own in production
  base_url: "http://localhost:8080/"
  # Disable rate limiter for CLI/Agent access (no JS needed)
  limiter: false
  # JSON output is enabled for seekit (see search.formats)
  image_proxy: false

# Keep only engines accessible from China without proxy
# (Google, DuckDuckGo, Brave require a proxy that is unreliable with httpx)

ui:
  static_use_hash: true
  default_locale: ""
  default_theme: simple
  theme_args:
    simple_style: dark

search:
  # Safe search: 0=off, 1=moderate, 2=strict
  safe_search: 0
  # Default search language (empty = no restriction)
  default_lang: "en"
  # Enable JSON output format for seekit
  formats:
    - html
    - json
  autocomplete: ""
  # Engine ban time on failure (seconds)
  ban_time_on_fail: 10
  max_ban_time_on_fail: 300

# Enabled engines: accessible without proxy from China
engines:
  # Bing
  - name: bing
    engine: bing
    shortcut: b
    disabled: false

  # Wikipedia
  - name: wikipedia
    engine: wikipedia
    shortcut: wp
    disabled: false

  # GitHub
  - name: github
    engine: github
    shortcut: gh
    disabled: false

  # Baidu
  - name: baidu
    engine: baidu
    shortcut: bd
    disabled: false

  # Sogou
  - name: sogou
    engine: sogou
    shortcut: sg
    disabled: false

  # Engines requiring proxy (disabled by default)
  - name: google
    engine: google
    shortcut: g
    disabled: true

  - name: duckduckgo
    engine: duckduckgo
    shortcut: ddg
    disabled: true

  - name: brave
    engine: brave
    shortcut: be
    disabled: true

  - name: yahoo
    disabled: true

  - name: yandex
    disabled: true

  - name: qwant
    disabled: true
